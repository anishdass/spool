server:
  port: 8080

spring:
  application:
    name: project-untitled
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org.springframework.security: DEBUG

tmdb:
  api:
    key:  3570b521118764bbb1a871af55e9fb5c
    url:  https://api.themoviedb.org/3

